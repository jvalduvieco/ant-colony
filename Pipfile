[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true
[scripts]
test = "pipenv run coverage run -m unittest discover -s test -t ."

[dev-packages]
coverage = "*"

[packages]

[requires]
python_version = "3.7"
